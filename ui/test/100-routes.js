describe('routes', function() {
    context('when the #/signup route is active', function() {
        describe('the document', function() {
            it('should display the signup container', function() {
                // TODO
            })
        })
    })

    context('when the #/signup route is inactive', function() {
        describe('the document', function() {
            it('should not display the signup container', function() {
                // TODO
            })
        })
    })
})
