export const ACCEPT_ATTEMPT = `${__filename}:ACCEPT_ATTEMPT`;
export const CHANGE_EMAIL_ADDRESS = `${__filename}:CHANGE_EMAIL_ADDRESS`;
export const CHANGE_PASSWORD = `${__filename}:CHANGE_PASSWORD`;
export const REJECT_ATTEMPT = `${__filename}:REJECT_ATTEMPT`;
export const SUBMIT_ATTEMPT = `${__filename}:SUBMIT_ATTEMPT`;
